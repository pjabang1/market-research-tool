{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeoIndicatorSeries list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Geoindicator_id</th>
                <th>Geography_id</th>
                <th>Value</th>
                <th>Date</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('geoindicatorseries_show', { 'id': entity.id }) }}">{{ entity.geoindicatorid }}</a></td>
                <td>{{ entity.geographyid }}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.date }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('geoindicatorseries_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('geoindicatorseries_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('geoindicatorseries_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
