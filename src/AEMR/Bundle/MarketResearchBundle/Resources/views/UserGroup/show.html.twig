{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserGroup</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Group_id</th>
                <td>{{ entity.groupid }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ entity.userid }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('usergroup') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('usergroup_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
