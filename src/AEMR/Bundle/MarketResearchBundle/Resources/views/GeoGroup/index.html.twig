{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeoGroup list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Type</th>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('geogroup_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('geogroup_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('geogroup_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('geogroup_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
