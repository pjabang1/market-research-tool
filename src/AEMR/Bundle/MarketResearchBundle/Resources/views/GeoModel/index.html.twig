{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeoModel list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Algorithm_code</th>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('geomodel_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.algorithmcode }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('geomodel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('geomodel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('geomodel_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
