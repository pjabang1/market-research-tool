{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeoModelParameterIndicator</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Geomodelparameter_id</th>
                <td>{{ entity.geomodelparameterid }}</td>
            </tr>
            <tr>
                <th>Geoindicator_id</th>
                <td>{{ entity.geoindicatorid }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ entity.weight }}</td>
            </tr>
            <tr>
                <th>Aggregation_type</th>
                <td>{{ entity.aggregationtype }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('geomodelparameterindicator') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('geomodelparameterindicator_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
