{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GeoModelParameterIndicator list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Geomodelparameter_id</th>
                <th>Geoindicator_id</th>
                <th>Weight</th>
                <th>Aggregation_type</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('geomodelparameterindicator_show', { 'id': entity.id }) }}">{{ entity.geomodelparameterid }}</a></td>
                <td>{{ entity.geoindicatorid }}</td>
                <td>{{ entity.weight }}</td>
                <td>{{ entity.aggregationtype }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('geomodelparameterindicator_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('geomodelparameterindicator_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('geomodelparameterindicator_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
